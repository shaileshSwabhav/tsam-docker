import { AllowNumbersWithDecimalDirective } from './allow-numbers-with-decimal.directive';

describe('AllowNumbersWithDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowNumbersWithDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
