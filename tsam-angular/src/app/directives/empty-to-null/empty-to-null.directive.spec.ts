import { EmptyToNullDirective } from './empty-to-null.directive';

describe('EmptyToNullDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyToNullDirective();
    expect(directive).toBeTruthy();
  });
});
